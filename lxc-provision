#!/bin/bash

if [ "$#" -ne 1 ]; then
    echo "You need to pass one parameter: the container's name or IP"
    exit 1
fi

ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no  -l aentos -i /var/cache/lxc/insecure_private_key $1 'sudo /opt/chef/bin/chef-solo -c /etc/chef/solo.rb'
